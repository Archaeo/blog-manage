{
  "name": "blog-manage",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter gamecodekr dev",
    "build": "pnpm --filter gamecodekr build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}
